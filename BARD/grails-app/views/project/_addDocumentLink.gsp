<g:link action="create" controller="document"
        params="${[projectId: "${projectId}", documentType: "${documentType}"]}"
        class="btn"><i class="icon-plus"></i> ${label}</g:link>
<br/><br/>
